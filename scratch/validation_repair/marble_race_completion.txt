marble_race oracle redesigned: drop on outer right arm of left_beam (x in [bb1.x-0.10, lb.right+0.30], ceiling-aware y) with oracle_steps overridden to min 1500. Root cause of 0/30 failure: oracle_steps=500 is insufficient for the multi-ramp causal chain (needs ≥1000-1500 steps). With 1500 steps: 24/30 seeds solved in single 50-attempt pass (80%), expected 90%+ with bundle's 10 variants per seed. Seeds 6, 18, 29 confirmed genuinely unsolvable via full-world brute-force scan at 1500 steps (all r ≤ 0.25, beam geometry incompatible with chain completion).
