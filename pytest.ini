[pytest]
testpaths = tests
djano_debug_mode = false
addopts = -ra
markers =
    fast: quick unit tests
    comprehensive: slower integration tests
    intervention: tests requiring intervention-enabled configs
